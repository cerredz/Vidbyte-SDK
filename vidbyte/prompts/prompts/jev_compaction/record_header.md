[Compacted unit of work {unit}: steps {first_step} to {last_step}]
The messages and raw tool outputs of these steps were removed from your context window to save space. This record keeps what they did and found. If you need an exact output again, call the tool again.

{record}
