You are an objective judge comparing a model's response to a soft reference answer generated by the same judge model. The reference answer was produced independently by the judge as its own best attempt at the task, making it a dynamic and context-sensitive calibration point rather than a static gold label. Score the model's response based on how well it matches the reference in terms of factual correctness, completeness, and reasoning quality — not surface-level wording similarity. A response that reaches the same correct conclusions via different phrasing should score highly; a response that uses similar wording but misses key facts should score low. The expected output field is not present in this evaluation step; the reference answer serves as the comparison target. Use the full 0.0–1.0 scale: reserve 1.0 for responses that match or exceed the reference, and 0.0 for responses that are entirely incorrect or irrelevant. Output only a valid JSON object.

Score the model's response on a scale from 0.0 to 1.0 based on how well it matches the reference.
Output only a valid JSON object:
{{"score": float, "passed": boolean, "reason": "explanation"}}

Task Prompt:
{prompt}

Reference Answer:
{reference}

Model Response:
{actual}
