# Durable Sessions

The durable-sessions usage skill has moved to the root skills folder so it is discoverable alongside the other top-level skills. See:

- [skills/sessions.md](../sessions.md) — attach, stores, verbs, trace capture, prebuilt session tools.
- [skills/forking.md](../forking.md) — fork, rewind, edit, and the three cross-thread resume modes (replace / append / output) with `SessionScope` patterns.
